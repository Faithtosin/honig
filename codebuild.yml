
version: 0.2
env:
  variables:
    UiBucket_name: $UiBucket_name
phases:
  install:
    commands:
      # Update libs
      - ls -la
      - ls -la honig
      - apt-get update
      - cd app
  pre_build:
    commands:
      - echo "pre-build"
  build:
    commands:
      - echo "pre-build"
  post_build:
    commands:
      -  echo "pre-build"
artifacts:
   files:
     - '**/*'
